# PROVENANCE LEDGER (internal — clean-room record)

> **Purpose.** This is the originality audit trail for a plugin intended for
> **free public release on GitHub** under a hard constraint: *everything must be
> original and license-clean; other plugins are inspiration only, never copied.*
> A public repo makes this non-negotiable — no third-party licensed code, no
> copyrighted text, no real client/brand names may ship. Keep this ledger current
> as assets are added; it is the document you point to if anyone questions
> provenance. ("License: proprietary" below records *original authorship by us* —
> the repo's distribution license is set separately in `LICENSE`.)

## Clean-room rules (enforced for every asset)

1. **Inspiration, not transcription.** Reading another plugin to understand a
   *capability* is fine. Copying its files, prompt text, data rows, or wording
   is not — even "rewritten lightly."
2. **No third-party names in shipped/user-facing files.** Source attribution
   (if legitimately required, e.g. CC BY) lives in `NOTICE.md`, never in skill
   names, descriptions, README marketing, or `plugin.json`.
3. **Data is generated, not lifted.** CSV libraries are produced from first
   principles or from openly-licensed primary sources, with the method recorded
   below. Matching another product's exact row counts is treated as a red flag.
4. **Standards are referenced, not redistributed.** Implement against WCAG /
   Schema.org / CWV; do not paste their text.

## Asset ledger

| Asset | Status | Author | Source basis | License | Notes |
|---|---|---|---|---|---|
| 45 SKILL.md bodies | original | Zachary W. | written from scratch | proprietary | capabilities informed by general domain knowledge |
| `scripts/design/design_system.py` | BUILT | Zachary W. | written from scratch | proprietary | stdlib only; deterministic scoring engine |
| `scripts/design/match.py` | BUILT | Zachary W. | written from scratch | proprietary | stdlib only (math/difflib/re); clean-room weighted-IDF ranker that replaces raw token-overlap behind the `DPS_RANKER` revert flag; deterministic |
| `scripts/design/gen_palettes.py` | BUILT | Zachary W. | written from scratch | proprietary | computes palettes via HSL + WCAG math; reproducible |
| `scripts/design/gen_charts.py` | BUILT | Zachary W. | written from scratch | proprietary | stdlib only; deterministic data-shape -> chart recommender over `data/chart-types.csv`; reuses `gen_palettes` WCAG luminance/contrast for series-color a11y; offline; JSON default + ASCII --human; bad input -> JSON error + non-zero |
| `scripts/design/a11y_static.py` | BUILT | Zachary W. | written from scratch | proprietary | stdlib only (`html.parser`); STRUCTURAL WCAG 2.2 checker over an HTML file/string -> severity-ranked JSON findings (img alt, heading order, form-control labels, html lang, skip link, main/landmark, viewport zoom, token-contrast cross-check); REUSES `gen_palettes.relative_luminance`/`contrast_ratio` for the WCAG math (one source of truth); deterministic + offline; bad input -> JSON error + non-zero |
| `scripts/workflow/portable_html.py` | BUILT | Zachary W. | written from scratch | proprietary | stdlib only; path-traversal-guarded inliner |
| `scripts/workflow/csv_to_report.py` | BUILT | Zachary W. | written from scratch | proprietary | stdlib only; sensitive-column redaction |
| `scripts/smoke_test.py` | BUILT | Zachary W. | written from scratch | proprietary | install verification |
| `scripts/seo/schema_gen.py` | BUILT | Zachary W. | written from scratch | proprietary | stdlib only; Schema.org JSON-LD generate + validate vs Google rich-results required/recommended props; encodes the 2026 FAQPage rich-result deprecation (spec referenced, not redistributed) |
| `scripts/seo/sitemap_tools.py` | BUILT | Zachary W. | written from scratch | proprietary | stdlib only; sitemaps.org-compliant generate/validate; enforces the 50,000-URL / 50 MB protocol limits |
| `scripts/seo/tech_audit.py` | BUILT | Zachary W. | written from scratch | proprietary | stdlib only; single-page technical SEO checks; encodes CWV targets (INP<200ms) + AI-crawler robots policy from public standards |
| `scripts/seo/geo_check.py` | BUILT | Zachary W. | written from scratch | proprietary | stdlib only; GEO passage-citability scoring + llms.txt/robots checks; implemented against public GEO findings (no third-party text) |
| `scripts/seo/hreflang_tools.py` | BUILT | Zachary W. | written from scratch | proprietary | stdlib only; hreflang validate/generate against ISO 639-1 / ISO 3166-1 code sets |
| `scripts/seo/drift_tools.py` | BUILT | Zachary W. | written from scratch | proprietary | stdlib only; SEO baseline capture + diff with severity classification |
| `scripts/design/render_page.py` | BUILT | Zachary W. | written from scratch | proprietary | stdlib only; renders an accessible single-file HTML page from the engine spec (WCAG-aware, prefers-reduced-motion, focus-visible) |
| `scripts/design/tokens_emit.py` | BUILT | Zachary W. | written from scratch | proprietary | stdlib only; emits CSS / Tailwind / SCSS / Style-Dictionary tokens from the engine spec |
| `scripts/workflow/capability_probe.py` | BUILT | Zachary W. | written from scratch | proprietary | stdlib only; detects available CLIs/env for the capability-tier cascade; never emits secret values; Codex-reviewed |
| `scripts/workflow/net_safety.py` | BUILT | Zachary W. | written from scratch | proprietary | stdlib only (ipaddress + socket); the one shared SSRF guard every URL-fetching script imports; blocks private/loopback/link-local/reserved/multicast/unspecified/non-global + cloud-metadata IPs (v4+v6), userinfo bypass, and DNS-rebinding via all-resolved-address checks |
| `scripts/workflow/cost_guard.py` | BUILT | Zachary W. | written from scratch | proprietary | stdlib only (argparse/json/datetime); FAIL-OPEN spend guard for paid connectors -- our own abstract-"credits" tier/estimate table + a local JSON ledger (NOT any vendor cost/cents schema); estimate/approve/log/total subcommands; a missing/corrupt/unreadable ledger or any guard error degrades to allow/free-Tier-2 (never blocks a deliverable); default ledger under the user home, never inside the plugin |
| `scripts/seo/page_fetch.py` | BUILT | Zachary W. | written from scratch | proprietary | stdlib only; SSRF-guarded single-URL fetcher (imports net_safety.validate_url on every hop); redirect-chain follow with max-hop cap + loop detection; flag-gated SPA-shell heuristic with a DISCLOSED UA-swap (never default-on); offline via --file / --no-network |
| `scripts/seo/site_map.py` | BUILT | Zachary W. | written from scratch | proprietary | stdlib only (xml.etree + html.parser + gzip); robots.txt + sitemap (urlset/index) discovery + on-page `<a href>` harvest -> classified URL inventory; every fetch gated by `net_safety.validate_url`; Tier-2 sampled-crawl mode states its sampling boundary; offline via `--file` / `--no-network`; written from scratch, no inspiration source opened |
| `scripts/seo/business_type.py` | BUILT | Zachary W. | written from scratch | proprietary | stdlib only; deterministic business-type + vertical classifier over provided on-page/schema/keyword signals; weighted-evidence rule table authored from first principles (no inspiration source opened); never fabricates (empty signals -> unknown/0.0); no network |
| `scripts/seo/crawl_inventory.py` | BUILT | Zachary W. | written from scratch | proprietary | stdlib only; deterministic post-processor that buckets a discovered-URL list into page-type/depth/host inventory via URL-path heuristics; does not fetch (offline); authored from first principles |
| `scripts/workflow/audit_aggregate.py` | BUILT | Zachary W. | written from scratch | proprietary | stdlib only; aggregates per-specialist scores into a health score RE-NORMALIZED over the specialists actually run (a skipped specialist is not a silent zero); own weight table; deterministic |
| `scripts/seo/serp_cluster.py` | BUILT | Zachary W. | written from scratch | proprietary | stdlib only (json/re/urllib.parse); deterministic SERP-overlap clustering over an orchestrator-supplied SERP blob (does NOT fetch — acquisition is a separate WebSearch/DataForSEO step); single-linkage components + SERP-centrality pillar selection + intent classifier + internal-link matrix; never fabricates volume/CPC/difficulty (emits `needs_tier1`); offline |
| `scripts/seo/geogrid.py` | BUILT | Zachary W. | written from scratch | proprietary | stdlib only (math/json/urllib.parse); deterministic Share-of-Local-Voice (SoLV) geo-grid math over SUPPLIED per-point rank data + an N x N center+radius grid builder; reciprocal-rank visibility, weighted mean, coverage/top-3/avg-rank; never fabricates (never-found grid scores 0); only `--geocode` fetches and routes through `net_safety.safe_open` (free OSM Nominatim); offline via `--grid` / `--center` / `--no-network` |
| `scripts/seo/nap_check.py` | BUILT | Zachary W. | written from scratch | proprietary | stdlib only (re/json); deterministic NAP (name/address/phone) consistency checker over supplied listings; first-principles normalization (phone digits + US country-code drop; token-wise street/directional/unit address map; name fold) so formatting-only differences are not flagged; diffs each listing vs a canonical + severity-ranked findings; optional `--verify-urls` fetch routes through `net_safety.safe_open`; offline by default |
| `scripts/verify_release.py` | BUILT | Zachary W. | written from scratch | proprietary | stdlib only; release-gate verifier (smoke + doc/provenance/clean-room/config consistency); Codex-reviewed |
| `templates/*.md` | BUILT | Zachary W. | written from scratch | proprietary | original framework wording |
| Evidence-led SEO methodology | original | Zachary W. | own loop + own prompts | proprietary | replaces third-party "FLOW" name/prompts |
| BLAST / 5-Dimensions framing | original content | Zachary W. | generic acronym, original sections | proprietary | rename if any TM concern surfaces |

### Per-CSV records (the `data/` libraries — built clean-room)

| File | Status | Rows | Method | Source / license |
|---|---|---|---|---|
| `color-palettes.csv` | BUILT (generated) | 24 | Produced by `gen_palettes.py` from 24 named industry/mood base hues via HSL rotation + WCAG contrast math. **Fully reproducible** — re-running the script reproduces the file byte-for-byte. | generated, proprietary |
| `ui-styles.csv` | BUILT (authored) | 18 | Enumerated from well-known, observable visual treatments; every characteristics/best-for/anti-pattern cell written for this plugin. | first-principles, proprietary |
| `font-pairings.csv` | BUILT (authored) | 22 | Pairings of openly-licensed Google Fonts with our own pairing rationale. No third-party pairing list used. | Google Fonts are OFL/Apache (referenced, not redistributed); rationale proprietary |
| `ux-rules.csv` | BUILT (authored) | 32 | Established usability principles restated in our own words with our own check/priority columns. | first-principles, proprietary |
| `product-types.csv` | BUILT (authored) | 26 | Product/page-type → default mappings authored from domain knowledge. | first-principles, proprietary |
| `competitor-rubric.csv` | BUILT (authored — W3b) | 18 | (a) Consulted: first principles + this plugin's own `design-research` scoring needs and the engine's field-weight ladders in `scripts/design/design_system.py`; **no inspiration source opened.** (b) Source text of the inspiration folder **NOT open** during authoring; **row count 18 chosen clear of every recorded `.cleanroom-thirdparty` ±2 band** (smallest inspiration count is 25, so ≤22 clears with margin) and clear of the column near-match check. (c) Independent basis: the 7-column schema (`dimension,category,weight,what_good_looks_like,what_bad_looks_like,signal_to_check,white_space_test`) is **derived from what a design-research pass actually scores** — the observable signal, its good/bad anchors, its weighted contribution, and the open-lane test — not any external CSV header; every cell authored from scratch. | first-principles, proprietary |

> Row counts reflect what was actually built (deliberately **not** matched to any
> other product's counts). To verify the generated palette file is original and
> reproducible: `python3 scripts/design/gen_palettes.py --print` regenerates it
> from the algorithm in the script.

## Data clean-room method (when filling `data/`)

The design-intelligence CSVs must be **independently constructed**, not copied:

- **UI styles** — enumerate from observable, well-known visual treatments
  (each row authored with our own characteristics/fit/anti-pattern columns).
- **Color palettes** — generate programmatically (HSL/contrast math in our own
  script) and/or curate from openly-licensed palette sources with attribution;
  record the generator/seed in this ledger.
- **Font pairings** — built from Google Fonts metadata (open) with our own
  pairing rationale; no third-party pairing list copied.
- **UX rules / product-type mappings** — written in our own words from
  established usability principles (Nielsen heuristics et al. are ideas, not
  protected expression — but the wording here is ours).

Record, per CSV, the generation date, method, and any primary source + its
license. **Row counts should reflect what we actually built, not a target
borrowed from another product.**

## Removed provenance language (history)

For the record, the following were removed during the Phase 1 quarantine because
they named third-party plugins in user-facing or product files:

- `plugin.json` description previously listed several third-party plugins by name.
- `README.md` / `CLAUDE.md` previously referenced specific source plugins.
- `data/README.md` previously stated the first CSV batch came from another
  plugin's source folder and declared row counts matching that product exactly.

These are documented here (internal) and scrubbed from all shipping files.

---

# Per-asset-class enrichment ledger (Wave 0 onward)

> **Added per MASTER-PLAN §7.2.** Everything above this line is preserved verbatim —
> the release gate's `check_prov` requires every shipped script to keep its exact
> backtick-delimited path entry, so the original Asset/CSV tables are never edited,
> only extended here. This part adds **per-asset-class sections** and **two new ledger
> columns** for every asset added from Wave 0 onward.

## New ledger columns

The enrichment format adds two columns to the older `Status | Author | Source basis |
License | Notes` shape:

- **Originality-check** — the originality verdict for the asset: `original`,
  `derived-guard` (a mechanical guard that holds no third-party expression), etc.
- **CLEAN-ROOM-METHOD** — the method note. For **high-risk classes** (prompts,
  per-skill references, CSVs, agents) this note is **mandatory** and must record
  **(a)** what was consulted (a capability bullet list only / a named PUBLIC standard /
  first principles), **(b)** explicit confirmation the source text was **NOT open**
  during authoring, and **(c)** the independent basis (e.g. "stages derived from our
  engine"; "columns derived from `design_system.py` field needs"). **A bare "original"
  is not sufficient for these classes.**

## Clean-room method per asset class (recipe)

Use this recipe to author the CLEAN-ROOM-METHOD note for each class. High-risk classes
must satisfy the (a)/(b)/(c) wall; low-risk classes record method + reuse only.

| Asset class | Risk | Required method note (recipe) |
|---|---|---|
| **Scripts** | low–med | First-principles algorithm description + the stdlib modules used + reuse of the shared backbone (`net_safety.validate_url`, `cost_guard`); determinism/SSRF/honesty reviewed by Codex. No (a)/(b)/(c) wall, but note "written from scratch; no inspiration source opened" for any script that mirrors an inspiration capability. |
| **CSV data** | HIGH | (a) what was consulted (open primary source / first principles), (b) source-text-not-open confirmation, (c) basis; **plus** the row-count and column-schema independence verdict against `.cleanroom-thirdparty` (no count within ±2 of a real inspiration count; no column-set near-match). |
| **Per-skill references** | HIGH | (a) public standard named, or "first principles", (b) source-text-not-open confirmation, (c) basis tied to the plugin's own scripts/contract. Knowledge-not-steps, <~200 lines. |
| **Agents** | HIGH | (a)/(b)/(c) **plus** "wraps the existing skill method, no forked logic"; least-privilege frontmatter; one agent per dispatched leaf, never for an orchestrator. |
| **Templates & prompt-libraries** | HIGH | (a)/(b)/(c) authored **source-closed** (the inspiration text NOT open); **no third-party attribution header, no sync script, no `prompts.lock`**; original wording only. |
| **Connector-wirings** | low | Record the env-only secret name(s), the mandatory shared `validate_url()` import, and cost-guard **fail-open**; confirm no third-party cost/cents table or schema was copied. |

## Scripts

The free-first backbone landed in **W1a**. Each backbone script keeps its exact
backtick path entry in the Asset ledger above (so `check_prov` finds it) **and** carries
a per-class row here with the Scripts recipe note from the table in
"Clean-room method per asset class" (first-principles algorithm + stdlib modules used +
reuse of the shared backbone `net_safety.validate_url`; determinism / SSRF / honesty
Codex-reviewed; "written from scratch, no inspiration source opened" for any script that
mirrors an inspiration capability). Remaining leaf scripts (`geogrid.py`, `nap_check.py`,
`serp_cluster.py`, `uniqueness.py`, etc.) land **with their consuming skill** in W3 / v1.1
and gain their row then. The seo-drift SQLite engine (`drift_baseline.py` /
`drift_compare.py` / `drift_history.py`) and its `drift_severity.py` rules-engine landed in
**W3a** (the seo-drift flagship-depth wave) and are recorded below.

| Asset | Asset class | Originality-check | CLEAN-ROOM-METHOD |
|---|---|---|---|
| `scripts/workflow/net_safety.py` | script (shared SSRF guard -- the backbone keystone) | original | First-principles SSRF guard authored from scratch. `validate_url()` resolves every candidate host and rejects private / loopback / link-local / reserved / multicast / unspecified / non-global + cloud-metadata addresses (IPv4 and IPv6), the `user@host` userinfo-bypass, and DNS-rebinding via an all-resolved-address check. stdlib only (`ipaddress`, `socket`, `urllib.parse`). The single guard every URL-fetching script imports as a local sibling. No inspiration source opened; SSRF coverage + determinism Codex-reviewed. |
| `scripts/seo/page_fetch.py` | script (fetch backbone) | original | First-principles single-URL fetcher authored from scratch: redirect-chain follow with a max-hop cap + loop detection, a flag-gated SPA-shell heuristic with a DISCLOSED UA-swap (never default-on), offline via `--file` / `--no-network`. Reuses the backbone -- imports and calls `net_safety.validate_url` on every hop (sibling bootstrapped onto `sys.path`). stdlib only (`urllib.request`, `urllib.parse`, `gzip`, `html`). No inspiration source opened; Codex-reviewed. |
| `scripts/seo/site_map.py` | script (fetch backbone) | original | First-principles discovery authored from scratch: robots.txt + sitemap (urlset / index) recursion + on-page `<a href>` harvest into a classified URL inventory; the Tier-2 sampled-crawl mode states its sampling boundary; offline via `--file` / `--no-network`. Every fetch gated by the shared `net_safety.validate_url`. stdlib only (`xml.etree.ElementTree`, `html.parser`, `gzip`, `urllib`). No inspiration source opened. |
| `scripts/workflow/cost_guard.py` | script (connector-safety backbone) | original | First-principles FAIL-OPEN spend guard authored from scratch: our own abstract-"credits" tier / estimate table + a local JSON ledger (NOT any vendor cost / cents schema); estimate / approve / log / total subcommands. A missing / corrupt / unreadable ledger or any guard error degrades to allow / free-Tier-2 -- it can never block a deliverable. stdlib only (`argparse`, `json`, `datetime`). No inspiration source opened; fail-open behavior Codex-reviewed. |
| `scripts/seo/business_type.py` | script (audit-math trio) | original | First-principles deterministic business-type + vertical classifier authored from scratch over provided on-page / schema / keyword signals; a weighted-evidence rule table written from first principles; never fabricates (empty signals -> unknown / 0.0); no network. stdlib only. No inspiration source opened. |
| `scripts/seo/crawl_inventory.py` | script (audit-math trio) | original | First-principles post-processor authored from scratch that buckets a discovered-URL list into page-type / depth / host inventory via URL-path heuristics; offline (does not fetch). stdlib only. No inspiration source opened. |
| `scripts/workflow/audit_aggregate.py` | script (audit-math trio) | original | First-principles aggregator authored from scratch that combines per-specialist scores into a health score RE-NORMALIZED over the specialists actually run (a skipped specialist is not a silent zero); own weight table; deterministic; no network. stdlib only. No inspiration source opened. |
| `scripts/workflow/capability_probe.py` | script (capability-tier cascade -- W1a extension) | original | Extended from scratch in W1a: pins `DEFAULT_ENV_VARS` to add `FIRECRAWL_API_URL`, `MOZ_API_KEY`, `BING_WEBMASTER_API_KEY`, and `CRUX_API_KEY` alongside the existing `DATAFORSEO_*` / `FIRECRAWL_API_KEY` / `GEMINI_API_KEY` / `GOOGLE_API_KEY` as presence-only booleans; `--capabilities` tier view, `scripts/` self-check, `--budget` passthrough. Never emits a secret VALUE. stdlib only. No inspiration source opened; Codex-reviewed. |
| `scripts/seo/drift_severity.py` | script (seo-drift flagship -- W3a; severity rules-engine) | original | First-principles change->severity engine authored from scratch. Our own 3-tier model (critical / high / advisory) whose rules (D1..D14) are **DERIVED FROM EXACTLY which on-page elements `drift_tools.capture()` already records** -- every captured field (meta_robots, canonical, title, h1, schema_blocks, meta_description, og_*, h2_count, word_count) maps to one numbered rule; the change->tier mapping, wording, numbering, and the 20% word-count noise floor are all ours. The 3-tier shape is a generic idea; the mapping is not. **SOURCE-CLOSED: no inspiration comparison-rules file was opened during authoring; basis = this plugin's own captured-element list.** Deterministic, stdlib only (`json`, `argparse`); determinism + the mapping to be Codex-reviewed (no self-review). |
| `scripts/seo/drift_baseline.py` | script (seo-drift flagship -- W3a; SQLite store + capture-and-store) | original | First-principles SQLite persistence layer authored from scratch. Stores each snapshot as a JSON blob with `url` / `label` / `captured_at` promoted to indexed columns + a `meta` schema-version table (rationale in `references/seo-drift/sqlite-schema.md`). **Reuses `drift_tools.capture()` as the one canonical capturer and `drift_tools.load_html()` as the one SSRF-guarded loader** (local `--file` or `--url` fetched through the shared `net_safety.safe_open` guard -- never a raw urlopen); writes under the user's workspace (`./.seo-drift/baselines.db` by default, never inside the plugin). Deterministic given `--captured-at`; bad input -> JSON error + non-zero. stdlib only (`sqlite3`, `json`, `datetime`). No inspiration source opened. |
| `scripts/seo/drift_compare.py` | script (seo-drift flagship -- W3a; baseline compare) | original | First-principles comparator authored from scratch: captures the current snapshot via `drift_tools` (SSRF-guarded), resolves a stored baseline from the `drift_baseline` SQLite store (by id / latest-label / latest-url / latest), and runs the `drift_severity` engine -> a severity-ranked change report. Reuses the shared store + rules engines (schema and mapping each live in one place). Offline; bad input (missing db / no matching baseline) -> JSON error + non-zero. stdlib only (`sqlite3`, `json`). No inspiration source opened. |
| `scripts/seo/drift_history.py` | script (seo-drift flagship -- W3a; list/trend) | original | First-principles reporter authored from scratch over the `drift_baseline` store (no network, no capture): `--list` enumerates stored baselines; `--trend` runs `drift_severity.evaluate` on each consecutive snapshot pair to emit a regression timeline (per-transition changed-count + critical/high/advisory tally). Reuses the shared store + rules engines. Offline; missing db -> JSON error + non-zero. stdlib only (`sqlite3`, `json`). No inspiration source opened. |
| `scripts/design/a11y_static.py` | script (design-accessibility flagship leaf -- W3b) | original | First-principles STRUCTURAL WCAG 2.2 checker authored from scratch; **SOURCE-CLOSED: no inspiration source opened.** (a) Consulted only the public WCAG 2.2 standard (success criteria + the 4.5:1 / 3:1 minimums) and this plugin's own `references/shared/wcag-contrast-rules.md`. (b) No third-party a11y-checker code, rule list, or example content was open during authoring. (c) Independent basis: a stdlib `html.parser` pass collects the structural facts, and the check catalog + severity mapping (critical/serious/moderate/minor) + WCAG-SC assignments are ours; the token-contrast cross-check's color slots (`text`/`bg`/`surface`/`primary`/`on_primary`/`accent`) are DERIVED from what `tokens_emit.py` / `gen_palettes.py` emit, and it REUSES `gen_palettes.relative_luminance` / `contrast_ratio` rather than re-implementing the WCAG math (one source of truth). Deterministic + offline; JSON-by-default + `--human` ASCII; bad input (missing/unreadable HTML, unparseable `--tokens`, argparse failure) -> JSON error + non-zero exit; `-h` exits 0. Golden-file tested (`tests/test_a11y_static.py`, 34 cases) + a golden example (`references/examples/design-accessibility/`). stdlib only (`html.parser`, `json`, `argparse`); example/fixture content uses only fictional `Zephyr Corp` / `acme.example`. Determinism + honesty to be Codex-reviewed. |
| `scripts/seo/serp_cluster.py` | script (seo-cluster flagship leaf -- W3) | original | First-principles SERP-overlap clustering authored from scratch; no inspiration source opened. The script is a DETERMINISTIC, OFFLINE post-processor over an orchestrator-supplied SERP blob (`{keyword: [ranked urls]}`) -- SERP ACQUISITION is a separate WebSearch/DataForSEO step, NOT this script (the honest clean-room split). Own algorithm: URL normalization (drop scheme/www/trailing-slash/query/fragment) -> pairwise shared-top-N overlap -> single-linkage connected components at a caller-chosen threshold -> SERP-centrality pillar selection (max summed sibling overlap, headier-term tie-break) -> a from-scratch keyword-modifier intent classifier (transactional/navigational/commercial/informational, most-specific-first) -> an internal-link matrix (spoke->pillar with the pillar keyword as anchor, pillar->spoke, spoke->top-3 siblings by overlap). Never fabricates volume/CPC/difficulty (emits a `needs_tier1` list). Golden-file tested (`tests/test_serp_cluster.py`); JSON-by-default + `--human` ASCII; bad input -> JSON error + non-zero exit; stdlib only (json/re/urllib.parse). Does not fetch, so it imports no net_safety guard. Determinism + honesty to be Codex-reviewed. |
| `scripts/seo/geogrid.py` | script (seo-local-unified flagship leaf -- W3) | original | First-principles Share-of-Local-Voice (SoLV) geo-grid engine authored from scratch; no inspiration source opened. Two modes: (1) SoLV math over a SUPPLIED grid of per-point ranks -- per-point visibility = reciprocal rank (1/r, 0 when not found within scan depth D), SoLV% = 100 * weighted-mean(visibility), plus coverage% / top-3% / avg-found-rank / best / worst; (2) an N x N center+radius grid BUILDER (equirectangular km->degree offsets, pole-singularity guarded). Live SERP rank ACQUISITION is a separate Tier-1 step, NOT this script (the honest clean-room split), so SoLV/build are fully OFFLINE + deterministic + golden-file tested (`tests/test_local_maps.py`). Never fabricates (a never-found grid scores 0). Only `--geocode` touches the network and routes through the shared `net_safety.safe_open` (free OSM Nominatim) -- never urlopen directly; `--no-network` refuses it. JSON-by-default + `--human` ASCII; bad input -> JSON error + non-zero exit. stdlib only (math/json/urllib.parse). Determinism + SoLV math to be Codex-reviewed. |
| `scripts/seo/nap_check.py` | script (seo-local-unified flagship leaf -- W3) | original | First-principles NAP (name/address/phone) consistency checker authored from scratch; no inspiration source opened. DETERMINISTIC + OFFLINE-by-default over caller-supplied listings: own normalization rules -- phone reduced to digits with a leading US country-code drop; address lowercased/punctuation-stripped with a token-wise street-type / directional / unit abbreviation map (USPS-style common forms, authored from first principles); name folded -- so cosmetic formatting differences are NOT flagged while real divergences are. Diffs every listing against a canonical (chosen by `--canonical` / `primary:true` / first), emits severity-ranked findings (phone/address HIGH, name MEDIUM) + a per-listing match matrix + per-field variant lists. The optional `--verify-urls` page check routes through the shared `net_safety.safe_open` -- never urlopen directly; `--no-network` skips it. Golden-file tested (`tests/test_local_maps.py`); JSON-by-default + `--human` ASCII; bad input -> JSON error + non-zero exit. stdlib only (re/json). Determinism + normalization rules to be Codex-reviewed. |
| `scripts/design/match.py` | script (design-engine ranker -- W1a design lane) | original | First-principles weighted ranker authored from scratch to replace `design_system._overlap` (raw token-overlap). Our own model: per-corpus IDF over row documents (idf(t)=ln((N+1)/(df+1))+1, so generic high-frequency tokens like "modern"/"clean" collapse toward weight 1.0 and specific tokens dominate -- the core fix), per-picker field-weight ladders (identity/name > mood/tags > prose), pivoted length-normalization (B=0.25), a difflib fuzzy pass (threshold 0.82) for near-misses/typos, and the kept-and-sharpened honesty: a CONFIDENCE_FLOOR (1.5; sits above the palette AA tie-break bonus) and a TIE_MARGIN (0.05) ambiguity flag. Deterministic (CSV-order tie-break; strictly-greater update; no dict-order dependence). All weights/params are ORIGINAL, tuned on THIS plugin's own corpus; **no inspiration source opened**. Wired into `design_system.py` behind the `DPS_RANKER` {new,legacy} flag (default new) as the one-release named revert path -- legacy reproduces the captured golden baseline byte-for-byte. stdlib only (math, difflib, re, collections); determinism / IDF math / confidence-floor to be Codex-reviewed (no self-review). |

## Scripts (design-charts flagship -- W3b)

| Asset | Asset class | Originality-check | CLEAN-ROOM-METHOD |
|---|---|---|---|
| `scripts/design/gen_charts.py` | script (design-charts flagship leaf -- W3b) | original | First-principles data-shape -> chart recommender authored from scratch; **SOURCE-CLOSED: no inspiration source (or its chart taxonomy) opened.** (a) Consulted only first principles (chart choice follows the relationship in the data) + the public WCAG 1.4.11 3:1 non-text-contrast bar + this plugin's own `data/chart-types.csv`, `gen_palettes.py`, and `render_page.py`. (b) No third-party chart library, chart taxonomy, or example content was open during authoring. (c) Independent basis: the shape->chart selection (exact-shape then volume-fit then a11y-grade then CSV order), the volume-overflow disclosure, and the A/B/C grading are ours; the series-color check REUSES `gen_palettes.relative_luminance` / `contrast_ratio` rather than re-implementing the WCAG math (one source of truth). Deterministic + offline; JSON-by-default + `--human` ASCII; bad input (unknown shape, unreadable CSV, argparse failure) -> JSON error + non-zero exit; `-h` exits 0. Golden-tested (`tests/test_gen_charts.py`). stdlib only (`csv`, `json`, `os`, `argparse` + the local `gen_palettes` sibling). Determinism + honesty to be Codex-reviewed. |

## CSV data

| File | Status | Rows | CLEAN-ROOM-METHOD | Source / license |
|---|---|---|---|---|
| `data/chart-types.csv` | BUILT (authored) | 40 | (a) Consulted: first principles (a data SHAPE determines the chart that makes its relationship legible) + the public WCAG 1.4.11 non-text-contrast bar; **no inspiration chart file or taxonomy opened.** (b) Source text/rows of any inspiration data file **NOT open** during authoring. (c) Independent basis: the 9-column schema (`data_shape,chart_type,best_for,min_series,max_series,max_points,a11y_grade,a11y_fallback,notes`) is DERIVED from what `gen_charts.py` / `render_page.py` consume (shape as the routing key, series/point volume thresholds, our own A/B/C a11y grade + fallback) -- not any external chart-taxonomy schema. **C-COUNT independence:** 40 rows is clear of every recorded inspiration on-disk row band (nearest are 34 and 44, both > +/-2 away; deliberately NOT near the 25-row inspiration chart file), and the snake_case column set shares no column with any inspiration header (near-match check clears). | first-principles, proprietary |
| `data/competitor-rubric.csv` | BUILT (authored -- W3b) | 18 | (a) Consulted: first principles + this plugin's own `design-research` scoring step and the engine field-weight ladders in `scripts/design/design_system.py`; **no inspiration source opened.** (b) Source text/rows of any inspiration data file **NOT open** during authoring. (c) Independent basis: the 7-column schema (`dimension,category,weight,what_good_looks_like,what_bad_looks_like,signal_to_check,white_space_test`) is DERIVED from what a design-research pass actually scores (the observable signal, its good/bad anchors, its weighted contribution, and the open-lane white-space test) -- not any external CSV header; every cell authored from scratch, examples using only fictional `.example` brands. **C-COUNT independence:** 18 rows is clear of every recorded inspiration on-disk row band (the smallest inspiration count is 25, band [23..27], so 18 clears with a 5-row margin below it), and the snake_case column set shares no column with any inspiration header (near-match check clears). | first-principles, proprietary |

> `chart-types.csv` maps a data shape -> recommended chart with volume thresholds, an
> a11y grade, and a fallback. `competitor-rubric.csv` holds the weighted, anchored
> dimensions a `design-research` pass scores a competitor set against. Both row counts
> (40, 18) and column sets are our own, chosen to feed the engine and to sit clear of
> every `.cleanroom-thirdparty` band (verified by C-COUNT).

*This CSV-data section now records both v1 differentiator CSVs (`chart-types.csv`,
`competitor-rubric.csv`); any future `cro-heuristics.csv` lands with its consuming skill.*
`competitor-rubric.csv` also keeps its row in the **Per-CSV records** table above
(unchanged). The 5 pre-existing `data/` CSVs remain recorded there (unchanged).

## Per-skill references & governance docs

| Asset | Asset class | Originality-check | CLEAN-ROOM-METHOD |
|---|---|---|---|
| `references/ENGINE-CONTRACTS.md` §11–§16 + §4 amendment + §1 Capability-routing placement | governance / authoring-contract doc | original | Authored from first principles against the plugin's own architecture and MASTER-PLAN §7.1 / §3 invariants / §11.2–§11.3; **no inspiration source opened.** Continues the existing 1–10 numbering in the same terse knowledge-not-steps voice. Records the agent layer (dispatched-leaf-only, no forked method, least-privilege frontmatter, Capability-routing + machine-parseable Output-contract blocks), per-skill references (earned/on-demand/<~200 lines/source-closed for high-risk), templates/prompts (original wording; no third-party attribution headers / sync scripts / `prompts.lock`), hooks spec-only (stdlib, exit 0/1/2, wrap validators, fail-open exit 0 on unparseable, never print secrets), connector/SSRF (one shared `validate_url` import, env-only secrets, cost-guard fail-open), and free-path-mandatory (real Tier-2 required for Stable; `seo-image-gen` exempt-by-spec). All edits additive; no existing line removed or renamed. |
| `references/CAPABILITY-TIERS.md` (+ "Capability families — the full tier map" section) | per-skill reference / governance doc | original | (a) Consulted: first principles + MASTER-PLAN §4 and §7.1 and the existing in-repo CAPABILITY-TIERS.md / ENGINE-CONTRACTS.md. (b) Source text of the inspiration folder **NOT open** during authoring. (c) Basis: capability slugs and Tier-2 backbone scripts derived from the MASTER-PLAN §4 script roster; tier columns and never-fabricate list derived from the plugin's own free-first contract. No third-party names, counts, or schemas consulted. |
| `references/CAPABILITY-TIERS.md` (+ "## Capability routing block — machine-parseable spec" section) | per-skill reference / governance doc | original | (a) Designed from first principles to satisfy ENGINE-CONTRACTS §1 canonical order (between Steps and Outputs) and §11/C4 (machine-parseable, Tier-2-non-empty). (b) No inspiration source consulted. (c) Basis: the 9-key capability-routing block grammar and the `seo-backlinks` worked example are authored from the plugin's own scripts and env-var probe set. |
| `references/skill-section-template.md` | per-skill reference / governance doc (W2 authoring template) | original | (a) Consulted: first principles + this repo's own ENGINE-CONTRACTS §1 (canonical section order) and CAPABILITY-TIERS.md; no inspiration source opened. (b) Source text of the inspiration folder **NOT open** during authoring. (c) Basis: pins the optional `## Capability routing` slot (between Steps and Outputs) and the scored `## Outputs` + graceful-degradation `## Error Handling` table shapes from the plugin's own free-path / never-fabricate contract; all cells authored from scratch as earned (not boilerplate) content. |
| `references/shared/cwv-thresholds.md` | per-skill reference (shared knowledge) | original | (a) Consulted: PUBLIC standard — Google Core Web Vitals public metric definitions and threshold bands (LCP < 2.5s, CLS < 0.1, INP < 200ms, 75th-percentile assessment, field-vs-lab distinction, TTFB/FCP/TBT diagnostics). (b) No third-party source text was open during authoring; no inspiration file consulted. (c) Independent basis: thresholds and concepts are public Google standards, restated in original wording and structured around this plugin's free-path / never-fabricate contract; all prose authored from domain knowledge of the published metrics. |
| `references/shared/wcag-contrast-rules.md` | per-skill reference (shared knowledge) | original | (a) Consulted: PUBLIC standard — W3C WCAG 2.x contrast success criteria (4.5:1 normal text, 3:1 large text and non-text/UI) and the public sRGB relative-luminance formula (channel normalize, gamma-linearize with the 0.03928/12.92 and ((c+0.055)/1.055)^2.4 transfer, 0.2126/0.7152/0.0722 weights, (L1+0.05)/(L2+0.05) ratio). (b) No third-party source text was open during authoring; no inspiration file consulted. (c) Independent basis: the ratios and formula are public W3C standards; wording, tables, and the tie to the plugin's deterministic `gen_palettes.py` luminance math are authored from first principles. |
| `references/shared/schema-catalog.md` | per-skill reference (shared knowledge) | original | (a) Consulted: PUBLIC vocabulary — Schema.org type/property vocabulary and Google's public rich-result required/recommended property guidance (Organization, WebSite, Article, FAQPage, Product/Offer/AggregateRating/Review, LocalBusiness, Event, Person, etc.), plus the public FAQPage rich-result narrowing. (b) No third-party source text was open during authoring; no inspiration file consulted. (c) Independent basis: type and property names are public Schema.org vocabulary; the catalog selection, required-vs-recommended framing, and validity rules are authored to match what `scripts/seo/schema_gen.py` emits/validates, in original wording. |
| `references/shared/eeat-criteria.md` | per-skill reference (shared knowledge) | original | (a) Consulted: PUBLIC concept — Google's public Experience/Expertise/Authoritativeness/Trust quality framing (Trust-centered) and the YMYL concept. (b) No third-party source text was open during authoring; no inspiration file consulted. (c) Independent basis: E-E-A-T is a public conceptual framework, not protected expression; restated here as original, checkable signal criteria (byline/date/sourcing/first-hand markers) the plugin can inspect deterministically, with all wording authored from first principles. |
| `references/seo-drift/severity-model.md` | per-skill reference (seo-drift flagship -- W3a) | original | (a) Consulted: first principles + this plugin's own `scripts/seo/drift_tools.py` captured-element list and `scripts/seo/drift_severity.py`; no inspiration comparison-rules source opened. (b) Source text of the inspiration folder **NOT open** during authoring (the rule COUNT is incidental; no external 17-rule band targeted). (c) Independent basis: the 3-tier model and every D1..D14 rule are **derived from exactly which elements `drift_tools.capture()` records** -- the change->severity mapping, numbering, wording, and the 20% word-count noise floor are the plugin's own. Knowledge-not-steps, <~200 lines. |
| `references/seo-drift/sqlite-schema.md` | per-skill reference (seo-drift flagship -- W3a) | original | (a) Consulted: first principles + the SQLite stdlib module and this plugin's own `drift_baseline.py` schema; no inspiration source opened. (b) No third-party source text was open during authoring. (c) Independent basis: the JSON-blob-snapshot + indexed-metadata-columns + meta/version-table design and its query patterns are authored from the engine's own needs (forward-compatible with `drift_tools` adding/removing captured elements, fast latest-per-URL and trend queries). Knowledge-not-steps, <~200 lines. |
| `references/seo-cluster/serp-overlap-method.md` | per-skill reference (seo-cluster -- W3) | original | (a) Consulted: first principles + general public SEO domain knowledge (the well-known practice that keywords co-ranking the same URLs share intent); no inspiration source opened. (b) Source text of the inspiration folder **NOT open** during authoring. (c) Independent basis: the overlap metric, the threshold/single-linkage method, the chaining caveat, and the SERP-centrality pillar rule all describe THIS plugin's own `scripts/seo/serp_cluster.py` algorithm; knowledge-not-steps, under ~200 lines. All wording authored from scratch. |
| `references/seo-cluster/hub-and-spoke.md` | per-skill reference (seo-cluster -- W3) | original | (a) Consulted: first principles + general public content-architecture knowledge (pillar/spoke, internal linking, anchor-text discipline); no inspiration source opened. (b) Source text of the inspiration folder **NOT open** during authoring. (c) Independent basis: the intent->page-type table, the link-matrix rules, and the sibling-selection cap all describe THIS plugin's own `serp_cluster.py` output and the `seo-content-brief` handoff; knowledge-not-steps, under ~200 lines. All wording authored from scratch. |
| `references/seo-local-unified/nap-normalization.md` | per-skill reference (seo-local-unified -- W3) | original | (a) Consulted: first principles + general public local-SEO domain knowledge (NAP consistency is a known local-ranking signal; USPS-style street/unit abbreviations are public) + the USPS-style common-abbreviation idea; no inspiration source opened. (b) Source text of the inspiration folder **NOT open** during authoring. (c) Independent basis: every normalization rule (phone digit + country-code drop, the address token map, the name fold) and every severity threshold describe THIS plugin's own `scripts/seo/nap_check.py` behavior; knowledge-not-steps, under ~200 lines. All wording authored from scratch. |
| `references/seo-local-unified/solv-geogrid-formula.md` | per-skill reference (seo-local-unified -- W3) | original | (a) Consulted: first principles + general public local-maps knowledge (geo-grid rank tracking and Share-of-Local-Voice are well-known local-SEO practices; reciprocal rank is a standard IR construct; equirectangular km->degree conversion is public geodesy); no vendor's specific SoLV formula or any inspiration source opened. (b) Source text of the inspiration folder **NOT open** during authoring. (c) Independent basis: the reciprocal-rank visibility model, the weighted-mean SoLV definition, the depth/coverage/top-3 metrics, and the grid-builder geometry all describe THIS plugin's own `scripts/seo/geogrid.py` math; knowledge-not-steps, under ~200 lines. All wording authored from scratch. |
| `references/shared/anti-slop-principles.md` | per-skill reference (shared knowledge) | original | (a) Consulted: first principles only — no external standard or source; the framing (slop = decision-free design; eight tells + intentional counter-moves; the swap test) is this plugin's own. (b) No third-party source text was open during authoring; no inspiration file consulted. (c) Independent basis: an original evaluative rubric authored from the author's own design reasoning, cross-linked to the plugin's existing deterministic scaffold and `wcag-contrast-rules.md`. |
| `references/design-system-gen/ranking.md` | per-skill reference (design flagship -- W3b) | original | (a) Consulted: first principles + this plugin's own `scripts/design/match.py` and `scripts/design/design_system.py` (our own assets, read directly); no inspiration source opened. (b) Source text of the inspiration folder **NOT open** during authoring. (c) Independent basis: every described mechanism (per-corpus smoothed IDF, the four per-picker field-weight ladders, the pivoted length-norm B=0.25, the 0.82 difflib fuzzy threshold, the 1.5 confidence floor and 0.05 tie margin, CSV-order determinism) restates THIS plugin's own ranker; the two worked examples are constructed from scratch over generic design vocabulary. Knowledge-not-steps, <~200 lines. All wording authored from scratch. |
| `references/design-build/anti-slop.md` | per-skill reference (design flagship -- W3b) | original | (a) Consulted: first principles + this plugin's own `references/shared/anti-slop-principles.md`, `references/shared/wcag-contrast-rules.md`, `data/ui-styles.csv` and `data/ux-rules.csv` (our own assets); general public data-viz good-practice (match mark to data shape, no chartjunk, redundant non-hue encoding); no inspiration source opened. (b) Source text of the inspiration folder **NOT open** during authoring. (c) Independent basis: the tell→build-move mapping applies the plugin's own shared rubric at build time (cross-linked, not restated) and the chart-handling guidance is authored from the plugin's own free-path / never-fabricate contract and the WCAG luminance math it already ships. Knowledge-not-steps, <~200 lines. All wording authored from scratch. |
| `references/design-build/chart-selection.md` | per-skill reference (design-charts flagship -- W3b) | original | (a) Consulted: first principles (chart choice follows the data relationship) + this plugin's own `data/chart-types.csv`, `scripts/design/gen_charts.py`, `gen_palettes.py`, `render_page.py`, and `references/shared/wcag-contrast-rules.md`; general public data-viz good-practice (match mark to shape, zero-baseline bars, non-hue redundancy); no inspiration source opened. (b) Source text/taxonomy of any inspiration chart file **NOT open** during authoring. (c) Independent basis: the shape→chart table, the volume-threshold flip rule, the A/B/C grade meanings, and the series-vs-background and full-pairwise (worst-pair) series-vs-series 3:1 contrast method all describe THIS plugin's own `gen_charts.py` behavior and the WCAG luminance it reuses from `gen_palettes.py`. Knowledge-not-steps, <~200 lines. All wording authored from scratch. |
| `references/design-visual-qa/visual-qa-rubric.md` | per-skill reference (design flagship -- W3b) | original | (a) Consulted: first principles + PUBLIC standard (W3C WCAG AA contrast ratios via `references/shared/wcag-contrast-rules.md`) + this plugin's own `references/shared/anti-slop-principles.md`; no inspiration source opened. (b) Source text of the inspiration folder **NOT open** during authoring. (c) Independent basis: the four evaluation axes (hierarchy/rhythm/contrast/restraint), the improvement/neutral/regression verdict model, and the critical/high/advisory severity ladder are authored from first principles as the visual (pixel-level) complement to the plugin's own decision-level anti-slop rubric; the contrast floor cites the plugin's own WCAG reference. Knowledge-not-steps, <~200 lines. All wording authored from scratch. |
| `references/design-research/competitor-rubric.md` | per-skill reference (design-research flagship -- W3b) | original | (a) Consulted: first principles + this plugin's own `data/competitor-rubric.csv` and the `design-research` scoring step (our own assets); no inspiration source opened. (b) Source text of the inspiration folder **NOT open** during authoring. (c) Independent basis: the 0–4 anchored scale, the weighted-mean composite formula, the six-category grouping, and the open-lane reading of the `white_space_test` column all describe THIS plugin's own rubric CSV and its `research/02`/`research/03` outputs; the worked example uses fictional `.example` brands. Knowledge-not-steps, <~200 lines. All wording authored from scratch. |
| `references/design-research/white-space-method.md` | per-skill reference (design-research flagship -- W3b) | original | (a) Consulted: first principles + general public design-craft knowledge (proximity/consistency grouping, comfortable measure ~45–75 chars, geometric spacing scales, vertical rhythm) + this plugin's own token-scale lineage in `scripts/design/render_page.py`; no inspiration source opened. (b) Source text of the inspiration folder **NOT open** during authoring. (c) Independent basis: the "space is a decision" first principle, the spacing-scale/measure/density diagnostic tests, and the scoring hook into `data/competitor-rubric.csv` brand-craft dimensions are authored from the plugin's own deterministic spacing contract; the worked example uses fictional `.example` brands. Knowledge-not-steps, <~200 lines. All wording authored from scratch. |
| `references/design-accessibility/structural-checks.md` | per-skill reference (design-accessibility flagship -- W3b) | original | (a) Consulted: PUBLIC standard — the W3C WCAG 2.2 success criteria (1.1.1, 1.3.1, 1.4.3/1.4.4/1.4.10/1.4.11, 2.4.1, 3.1.1, 3.3.2, 4.1.2) and this plugin's own `references/shared/wcag-contrast-rules.md`; **no inspiration a11y-checker, rule list, or example content opened.** (b) Source text of the inspiration folder **NOT open** during authoring. (c) Independent basis: the check catalog, the four-tier critical/serious/moderate/minor severity mapping, the WCAG-SC assignments, the accessible-name decision (placeholder deliberately rejected, an EMPTY `<label for>`/`aria-labelledby` target is not a name), the working-skip-link rule (target id must exist), and the token-contrast cross-check's color slots (`text`/`bg`/`surface`/`primary`/`on_primary`/`accent`) all describe THIS plugin's own `scripts/design/a11y_static.py` behavior and the slots `tokens_emit.py` / `gen_palettes.py` emit; the contrast math is cited from the shared WCAG reference, not restated. Cited by `skills/design-accessibility/SKILL.md` (C1). Knowledge-not-steps, <~200 lines. All wording authored from scratch. |
| `references/geo-scorecard.md` | per-skill reference (seo-geo -- GEO scorecard rationale) | original | (a) Consulted: first principles + PUBLIC signals (robots.txt `User-agent` grouping semantics; the named AI retrieval-vs-training crawler user-agents as a public convention; Schema.org / llms.txt presence as machine-readability signals) and this plugin's own `scripts/seo/geo_check.py` (`SCORECARD_WEIGHTS`, the passage rubric, and the crawler-verdict table). (b) Source text of the inspiration folder **NOT open** during authoring; **no third-party GEO method, weighting, or scoring rubric consulted.** (c) Independent basis: every category weight, the four passage axes (sourced / self-contained / answer-first / concise) and their integer scoring, the `citable` boolean contract, the re-normalization-over-available-weight rule, the grade bands, and the retrieval-vs-training crawler-verdict table all describe THIS plugin's own `geo_check.py` behavior; the worked examples use only fictional brands. Cited by `skills/seo-geo/SKILL.md` (C1). Knowledge-not-steps, <~200 lines. All wording authored from scratch. |
| `references/seo-audit/dispatch-matrix.md` | per-skill reference (seo-audit orchestrator wiring -- W4) | original | (a) Consulted: first principles + this plugin's own `skills/seo-audit/SKILL.md`, `scripts/seo/business_type.py`, and `scripts/workflow/audit_aggregate.py` (our own assets, read directly); no inspiration source opened. (b) Source text of any inspiration audit/dispatch file **NOT open** during authoring. (c) Independent basis: the always/conditional split (5 + 6) is DERIVED from this plugin's own specialist set -- five universally-relevant on-page/architecture checks that run every audit plus six gated on business type / connected tooling -- and is explicitly NOT a third-party 8-always + 7-conditional dispatch shape (the C3 dispatch-shape guard enforces this). Cited by `skills/seo-audit/SKILL.md` (C1). Knowledge-not-steps. All wording authored from scratch. |
| `references/seo-audit/scoring-weights.md` | per-skill reference (seo-audit health-score model -- W4) | original | (a) Consulted: first principles + this plugin's own `scripts/workflow/audit_aggregate.py` (`DEFAULT_WEIGHTS`, the re-normalization formula, the A-F grade bands) -- our own asset, read directly; no inspiration source opened. (b) Source text of any inspiration scoring rubric **NOT open** during authoring; no third-party weighting consulted. (c) Independent basis: the per-specialist weight table, the re-normalize-over-present-specialists rule, the grade bands, and the never-fabricate / connector-gated honesty rules all describe THIS plugin's own `audit_aggregate.py` behavior; the worked example is constructed from scratch. Cited by `skills/seo-audit/SKILL.md` (C1). Knowledge-not-steps. All wording authored from scratch. |

## Agents

The agent layer (`agents/`) opened in **W2** with the authoring template plus one proof
leaf; the remaining agents are authored **just-in-time** in the wave that wires them
(W4 / v1.1). Each agent row carries the Agents recipe note ((a)/(b)/(c) + "wraps the
existing skill method, no forked logic" + least-privilege frontmatter).

| Asset | Asset class | Originality-check | CLEAN-ROOM-METHOD |
|---|---|---|---|
| `agents/_TEMPLATE.md` | agent scaffold (authoring template -- NOT a dispatched leaf; `_`-prefixed so C3/C5 treat it as scaffold) | original | (a) Consulted: first principles + this repo's own `references/ENGINE-CONTRACTS.md` §11 and `references/CAPABILITY-TIERS.md`; no inspiration source opened. (b) Source text of the inspiration folder **NOT open** during authoring. (c) Basis: the required-frontmatter list (name/description/model/maxTurns/least-privilege tools), the `## Capability routing` block, and the machine-parseable `## Output contract` block are derived from the plugin's own §11 agent contract and the C3/C5 gate. The template itself wraps no skill method -- it is the fill-in shape every real leaf adopts under the rule "wraps the existing skill method, no forked logic." All wording authored from scratch. |
| `agents/seo-technical.md` | agent (dispatched leaf -- the W2 proof instance) | original | (a) Consulted: first principles + the existing `skills/seo-technical/SKILL.md` (our own asset, read directly) and `references/ENGINE-CONTRACTS.md` §11; no inspiration source opened. (b) Source text of the inspiration folder **NOT open** during authoring. (c) Basis: **wraps the existing `seo-technical` skill method with no forked logic** -- same `cwv-field` Tier cascade, same `scripts/seo/tech_audit.py` invocation, same 9-dimension key-absent free path; adds only the machine-parseable `## Output contract` for deterministic orchestrator fan-in. Least-privilege frontmatter (Read/Glob/Grep/Bash/Write: Bash runs the bundled audit which performs the fetch, so no WebFetch/WebSearch; no Task -- agents never dispatch agents). One agent for one dispatched leaf, never for an orchestrator. |
| `agents/seo-page.md` | agent (dispatched leaf -- W4 always-on) | original | (a) Consulted: the existing `skills/seo-page/SKILL.md` (our own asset) + ENGINE-CONTRACTS §11; no inspiration source opened. (b) Inspiration agent/subagent source text **NOT open**. (c) Basis: **wraps the `seo-page` skill method, no forked logic** -- same free path (built-in WebFetch page fetch + on-page/meta/schema/image/link review), same outputs; adds the machine-parseable `## Output contract`. Least-privilege: Read/WebFetch/Grep (fetch-only -- fetches inline, so no Bash); field CWV deferred to `seo-google` as `needs_tier1`. Dispatched by `seo-audit` (always-on) + `qa-gate` phase 7. No Task. |
| `agents/seo-schema.md` | agent (dispatched leaf -- W4 always-on) | original | (a) Consulted: `skills/seo-schema/SKILL.md` + ENGINE-CONTRACTS §11; no inspiration source opened. (b) Inspiration source text **NOT open**. (c) Basis: **wraps the `seo-schema` method, no forked logic** -- same `scripts/seo/schema_gen.py` Tier-2 validator, same outputs; adds `## Output contract`. Least-privilege: Read/Glob/Grep/Bash (Bash runs the bundled validator; no fetch tool). Dispatched by `seo-audit` (always-on). No Task. |
| `agents/seo-sitemap.md` | agent (dispatched leaf -- W4 always-on) | original | (a) Consulted: `skills/seo-sitemap/SKILL.md` + ENGINE-CONTRACTS §11; no inspiration source opened. (b) Inspiration source text **NOT open**. (c) Basis: **wraps the `seo-sitemap` method, no forked logic** -- same `scripts/seo/sitemap_tools.py` + `site_map.py` Tier-2, same outputs; adds `## Output contract`. Least-privilege: Read/Glob/Grep/Bash (bundled scripts do the fetch). Dispatched by `seo-audit` (always-on). No Task. |
| `agents/seo-image-audit.md` | agent (dispatched leaf -- W4 always-on) | original | (a) Consulted: `skills/seo-image-audit/SKILL.md` + ENGINE-CONTRACTS §11; no inspiration source opened. (b) Inspiration source text **NOT open**. (c) Basis: **wraps the `seo-image-audit` method, no forked logic** -- same built-in image scan + per-image scoring, same outputs; adds `## Output contract`. Least-privilege: Read/Glob/Grep/WebFetch (fetch-only); image-SERP rankings deferred as `needs_tier1`. Dispatched by `seo-audit` (always-on). No Task. |
| `agents/seo-content.md` | agent (dispatched leaf -- W4 conditional) | original | (a) Consulted: `skills/seo-content/SKILL.md` + ENGINE-CONTRACTS §11; no inspiration source opened. (b) Inspiration source text **NOT open**. (c) Basis: **wraps the `seo-content` method, no forked logic** -- same `scripts/seo/geo_check.py --content` citability scorer + by-hand E-E-A-T/readability/depth, same outputs; adds `## Output contract`. Least-privilege: Read/Glob/Grep/Bash (scorer does any fetch). LLM-mention data as `needs_tier1`. Dispatched by `seo-audit` (conditional). No Task. |
| `agents/seo-geo.md` | agent (dispatched leaf -- W4 conditional) | original | (a) Consulted: `skills/seo-geo/SKILL.md` + ENGINE-CONTRACTS §11; no inspiration source opened. (b) Inspiration source text **NOT open**. (c) Basis: **wraps the `seo-geo` method, no forked logic** -- same `scripts/seo/geo_check.py` weighted scorecard + crawler-policy verdict + passage citability, same outputs; adds `## Output contract`. Least-privilege: Read/Glob/Grep/Bash (script fetches llms.txt/robots). Dispatched by `seo-audit` (conditional). No Task. |
| `agents/seo-local-unified.md` | agent (dispatched leaf -- W4 conditional) | original | (a) Consulted: `skills/seo-local-unified/SKILL.md` + ENGINE-CONTRACTS §11; no inspiration source opened. (b) Inspiration source text **NOT open**. (c) Basis: **wraps the `seo-local-unified` method, no forked logic** -- same `scripts/seo/nap_check.py` + `geogrid.py` (free Nominatim SoLV), same outputs; adds `## Output contract`. Least-privilege: Read/Glob/Grep/Bash/Write (scripts fetch; Write files the local report). Live rank / review ratings as `needs_tier1`. Dispatched by `seo-audit` (conditional). No Task. |
| `agents/seo-ecommerce.md` | agent (dispatched leaf -- W4 conditional) | original | (a) Consulted: `skills/seo-ecommerce/SKILL.md` + ENGINE-CONTRACTS §11; no inspiration source opened. (b) Inspiration source text **NOT open**. (c) Basis: **wraps the `seo-ecommerce` method, no forked logic** -- same `scripts/seo/schema_gen.py --validate` Product/Offer check + on-page product/category audit, same outputs; adds `## Output contract`. Least-privilege: Read/Glob/Grep/Bash. Shopping/marketplace/pricing as `needs_tier1`. Dispatched by `seo-audit` (conditional). No Task. |
| `agents/seo-google.md` | agent (dispatched leaf -- W4 conditional) | original | (a) Consulted: `skills/seo-google/SKILL.md` + ENGINE-CONTRACTS §11; no inspiration source opened. (b) Inspiration source text **NOT open**. (c) Basis: **wraps the `seo-google` method, no forked logic** -- same free-path `scripts/seo/tech_audit.py` + `geo_check.py` with the key-absent lab targets as the default deliverable, same outputs; adds `## Output contract`. Least-privilege: Read/Glob/Grep/Bash. Field CWV / GSC / GA4 as `needs_tier1`. Dispatched by `seo-audit` (conditional). No Task. |
| `agents/seo-backlinks.md` | agent (dispatched leaf -- W4 conditional) | original | (a) Consulted: `skills/seo-backlinks/SKILL.md` + ENGINE-CONTRACTS §11; no inspiration source opened. (b) Inspiration source text **NOT open**. (c) Basis: **wraps the `seo-backlinks` method, no forked logic** -- same built-in WebSearch linking-domain / mention discovery free path (the skill ships no backlink fetcher), same qualitative referring-domain profile; adds `## Output contract`. Least-privilege: Read/Grep/WebSearch (fetch-only; no Bash -- no bundled script). Backlink/referring-domain counts + authority as `needs_tier1`. Dispatched by `seo-audit` (conditional). No Task. |
| `agents/design-accessibility.md` | agent (dispatched leaf -- W4 design) | original | (a) Consulted: `skills/design-accessibility/SKILL.md` + ENGINE-CONTRACTS §11; no inspiration source opened. (b) Inspiration source text **NOT open**. (c) Basis: **wraps the `design-accessibility` method, no forked logic** -- same `scripts/design/a11y_static.py` stdlib structural checker (alt/heading/label/lang/skip-link/landmark/token-contrast), same severity-ranked findings; adds `## Output contract`. Least-privilege: Read/Glob/Grep/Bash (script reads local HTML; no fetch tool). Dispatched by `qa-gate` phase 3. No Task. |
| `agents/design-visual-qa.md` | agent (dispatched leaf -- W4 design) | original | (a) Consulted: `skills/design-visual-qa/SKILL.md` + ENGINE-CONTRACTS §11; no inspiration source opened. (b) Inspiration source text **NOT open**. (c) Basis: **wraps the `design-visual-qa` method, no forked logic** -- same Tier-1 Playwright screenshots/diffs with a structured-summary free path, same outputs; adds `## Output contract`. Least-privilege: Read/Glob/Grep/Bash/Write (Write files the visual-QA artifacts; Playwright is an MCP extension, not a frontmatter tool -- no fetch tool alongside Bash). Dispatched by `parallel-build` (per-variant compare) + `qa-gate` phases 2/8. No Task. |

## Templates & prompt-libraries

The `seo-strategy` evidence-led prompt library landed in **W3c**, authored
**source-closed**. Each row carries the Templates recipe note: (a)/(b)/(c) + "no
third-party attribution header, no sync script, no `prompts.lock`."

| Asset | Asset class | Originality-check | CLEAN-ROOM-METHOD |
|---|---|---|---|
| `skills/seo-cluster/templates/cluster-map.html` | template (interactive visualization -- W3) | original | (a) Consulted: first principles + standard self-contained HTML/SVG/vanilla-JS technique; no inspiration source opened. (b) Source text / markup of any inspiration HTML **NOT open** during authoring -- no structure, element IDs, or class names reused (all IDs/classes are `pm-`-prefixed and authored from scratch). (c) Independent basis: renders the JSON shape that THIS plugin's `scripts/seo/serp_cluster.py` emits (clusters/spokes/internal_links/singletons/params) via a literal `/*__SERP_CLUSTER_JSON__*/` injection marker; zero dependencies, no network, no build step. No third-party attribution header, no sync script, no `prompts.lock`. All markup/CSS/JS authored from scratch. |
| `references/seo-strategy/prompts/README.md` | template / prompt-library (evidence-led SEO prompts -- the cited library index; W3c) | original | (a) Consulted ONLY a capability bullet list of THIS plugin's OWN SEO tools -- `site_map.py`/`crawl_inventory.py`/`business_type.py` (Baseline inventory + classify), `tech_audit.py`/`geo_check.py`/`audit_aggregate.py` (Diagnose), `serp_cluster.py` (Cluster), `seo-content-brief`/`schema_gen.py`/`sitemap_tools.py` (Produce), `drift_baseline.py`/`drift_compare.py`/`drift_history.py`/`geogrid.py` (Verify) -- plus first principles. (b) The third-party FLOW prompt-library source text was **NOT open** at any point during authoring (authored source-closed). (c) Independent basis: the six loop stages (Baseline / Diagnose / Cluster / Prioritize / Produce / Verify) are DERIVED FROM this engine's own distinct evidence subsystems -- one stage per subsystem -- NOT a relabel of any external four-stage set and NOT mirroring any external per-stage prompt distribution; the five industry-vertical templates are derived from this plugin's OWN audience via `business_type.py` output (regrouped by strategic posture; count not fixed to any external vertical set); all prompt wording is original. Per the Templates recipe: no third-party attribution or open-license banner, no sync fetcher script, and no prompt-lock file (git history is the integrity record). The README is the SINGLE cited library entry (satisfies C1 + the C8 cited-reference check with one row) so the per-stage and vertical prompt files need no separate citation. |

## Connector-wirings

*No Wave 0 assets in this class.* Connector wirings (DataForSEO, Firecrawl, Google/GSC,
Semrush, Moz, Bing) are registered in **W4** under the open-ended roster. Each row
records env-only secret name(s), the mandatory shared `validate_url()` import, and
cost-guard **fail-open** — and confirms no third-party cost/cents table or schema was
copied.

## Attribution / NOTICE

| Asset | Asset class | Originality-check | CLEAN-ROOM-METHOD |
|---|---|---|---|
| `NOTICE.md` (Common Crawl Terms-of-Use attribution paragraph) | attribution / NOTICE | original | (a) Consulted: PUBLIC terms — the Common Crawl Foundation public Terms of Use (commoncrawl.org/terms-of-use) and the fact that Common Crawl is an openly published web-graph/index dataset. (b) No third-party source text was open during authoring; the ToU page text was not copied — only its existence, URL, and the query-not-redistribute obligation are referenced. (c) Independent basis: an original attribution paragraph stating the plugin queries-not-redistributes Common Crawl data, honors the ToU, ships no Common Crawl files, and degrades gracefully; appended additively with all prior `NOTICE.md` content preserved. |

## Docs (platform-neutral agent guide)

| Asset | Asset class | Originality-check | CLEAN-ROOM-METHOD |
|---|---|---|---|
| `AGENTS.md` | doc | original | Clean-room authored from first principles; mirrors the role and core rules of the plugin's own `CLAUDE.md` (read directly) and `ENGINE-CONTRACTS.md` for house conventions, but every sentence is original wording. Reframed platform-neutrally for any agent (Codex/Gemini/Cursor/etc.). Did **NOT** open the inspiration folder; glanced at the workspace-root `AGENTS.md` for tone only, copied nothing and carried over no workspace-only/client-specific content. |

## Clean-room CI guard records

| Asset | Asset class | Originality-check | CLEAN-ROOM-METHOD |
|---|---|---|---|
| `.cleanroom-thirdparty` | clean-room CI guard record (gitignored, non-shipping) | derived-guard — first-principles JSON authored by this agent; holds **NO** inspiration data rows or body text, only mechanical on-disk row counts + column-header names + a literal name/marker denylist + an owner-tool allowlist; mirrors the existing local `.clean-room-denylist` convention. | Counts and column headers extracted by a stdlib `csv.reader` pass that reads ONLY the header line and tallies remaining rows (no data cell copied). Name/marker denylist literals transcribed from MASTER-PLAN §7.3 (the plan itself, **not** the inspiration sources); bare lowercase `flow` deliberately excluded per spec. Allowlist fixed by task. Loaded at runtime by `scripts/verify_release.py` for checks C7/C-COUNT, the same way the clean-room check loads `.clean-room-denylist`; gitignored so the literals never ship publicly. |
